jdk:
  - openjdk17

android:
  components:
    - build-tools-34.0.0
    - android-34
    - platform-tools

env:
  SKIP_APP: "1"

install:
  - ./gradlew :Linq:publishReleasePublicationToMavenLocal -x test -x lint
